# Gradio-Stack

how to use this app.

1. go to bash 
2. then type source .venv/Scripts/activate
3. python .venv/Scripts/activate

to log into admin:
ADMIN_EMAIL=admin@site.com  
ADMIN_PASSWORD=admin123


problems with this app
I tried to install postgres but during installation I changed the listening port as i wanted to standardly use port 5000 for databases. however this would have adverse affects as I spent too long trying to get postgres working as I had a port listening errors, so my only other option was to either reinstall it again or switch, so i switched to SQLLite which was much easier to use as i just installed the dependency and then ran a terminal command and it worked


What components are in this stack.
